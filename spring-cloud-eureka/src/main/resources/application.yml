server:
  port: 8761
spring:
  application:
    name: eureka-serve
eureka:
  server:
    enable-self-preservation: false
  client:
    #不向注册中心注册自己
    register-with-eureka: false
    service-url:
      defaultZone: http://localhost:8761/eureka/